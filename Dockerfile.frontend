FROM node:18-alpine

WORKDIR /app
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ ./
EXPOSE 5173
# Proxy target when running in Docker (backend service)
ENV VITE_PROXY_TARGET=http://backend:8000
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
